package com.fengxiaoshuai.inheritdemo;

public class InheritDemo1 {
    // 封装:对象代表什么,就得封装对应的数据,并提供数据对应的行为。
    // 继承(extends关键字):让一个类与另一个类建立起继承关系。
    // public class Student extends Person{}
    // Student称为子类(派生类),Person称为父类(基类或超类)。
    // 把多个子类中重复的代码抽取到父类中,提高代码的复用性。
    // 子类可以在父类的基础上,增加其他功能,使子类更强大。
    // 当类与类之间,存在相同(共性)的内容,并满足子类是父类中的一种,就可以使用继承,来优化代码。

    // Java只支持单继承(一个子类只能继承一个父类),不支持多继承(子类不能同时继承多个父类),但支持多层继承。
    // 多层继承:子类A可以继承父类B,父类B可以继承父类C。
    // 每一个类都直接或间接的继承于Object。
    //
}
